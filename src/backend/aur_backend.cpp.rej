@@ -270,6 +270,18 @@
     colors::print_substatus("Downloading sources...");
     
     for (const auto& raw_url : info.source) {
+        std::string url = raw_url;
+        
+        auto pos = url.find("::");
+        if (pos != std::string::npos) {
+            url = url.substr(pos + 2);
+        }
+
+        if (url.find("$url") != std::string::npos) {
+            size_t p = url.find("$url");
+            url.replace(p, 4, info.url);
+        }
+
         // Simple check for git sources
         if (url.find("git+") == 0 || url.find("git://") == 0) {
             std::string clean_url = url;
